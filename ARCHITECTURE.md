# Ynem — Архитектура системы

Telegram-бот + Mini App: локальный дисконт-клуб (Экибастуз). Роли: покупатель, партнёр, админ.

**Стек:** Python 3.12, aiogram 3.x, Telegram Mini App (React/vanilla), Supabase (Postgres + Auth + Storage), деплой — VPS или Railway/Fly.io.

---

## 1. Компоненты

```
Telegram ──► aiogram-бот (Python) ──► Supabase Postgres
   │              │                        ▲
   │              ├─ QR-генератор          │
   │              └─ Планировщик           │
   │                 (APScheduler)         │
   └─► Mini App (WebApp) ── REST/Supabase ─┘
              (каталог скидок, кабинет партнёра)
```

| Компонент | Ответственность |
|---|---|
| **aiogram-бот** | Регистрация, платежи (чек), выдача QR, ввод кода партнёром, админ-команды, рассылки |
| **Mini App** | Витрина скидок (красивый каталог), кабинет партнёра со статистикой, админ-панель |
| **Supabase Postgres** | Все данные. RLS не нужен на старте — бот ходит через service key |
| **Supabase Storage** | Скрины чеков Kaspi, логотипы партнёров |
| **APScheduler** | Знак дня партнёрам, деактивация просроченных подписок, розыгрыш по расписанию, протухание QR |

Что в боте, а что в Mini App: **все действия** (оплата, QR, ввод кода) — в боте, кнопками. **Просмотр** (каталог скидок, статистика, списки) — в Mini App. Так MVP работает даже без Mini App вообще.

---

## 2. Схема БД

```sql
users (
  id            bigint PK,            -- telegram_id
  username      text,
  full_name     text,
  phone         text,
  role          text DEFAULT 'buyer', -- buyer | partner | admin
  referrer_id   bigint,               -- кто пригласил (ref_ deep link)
  lang          text DEFAULT 'ru',
  is_banned     bool DEFAULT false,
  created_at    timestamptz
)

partners (
  id            serial PK,
  user_id       bigint FK -> users,   -- владелец кабинета
  name          text,                 -- название бизнеса
  category      text,                 -- еда, красота, авто...
  address       text,
  discount_premium int DEFAULT 10,    -- % для подписчиков (10–15)
  avg_check     int,                  -- средний чек, для счётчика «вы сэкономили»
  work_hours    text,
  is_active     bool DEFAULT true,    -- админ выключил
  is_paused     bool DEFAULT false    -- партнёр сам на паузе
)

partner_staff (                        -- кассиры заведения (миграция 004)
  partner_id    int FK -> partners,    -- ON DELETE CASCADE
  user_id       bigint FK -> users,    -- UNIQUE: кассир ровно одного заведения
  added_at      timestamptz,
  PRIMARY KEY (partner_id, user_id)
)

staff_invites (                        -- инвайт-ссылки кассиров (миграция 005)
  token         text PK,               -- deep link t.me/<bot>?start=staff_<token>
  partner_id    int FK -> partners,    -- ON DELETE CASCADE
  expires_at    timestamptz,           -- TTL 24 ч
  used_by       bigint FK -> users     -- одноразовость: гасится атомарным UPDATE
)

partner_edits (                        -- заявки на изменение карточки (миграция 006)
  id            serial PK,
  partner_id    int FK -> partners,    -- UNIQUE WHERE pending: одна заявка на модерации
  proposed_by   bigint FK -> users,
  changes       jsonb,                 -- поле → новое значение (name/category/address/work_hours/avg_check)
  status        text,                  -- pending | approved | rejected
  decided_at    timestamptz, decided_by bigint
)

subscriptions (
  id            serial PK,
  user_id       bigint FK -> users,
  status        text,                 -- pending | active | expired | rejected
  receipt_url   text,                 -- скрин чека в Storage
  amount        int DEFAULT 2000,
  paid_at       timestamptz,
  expires_at    timestamptz,          -- paid_at + 30 дней
  confirmed_by  bigint                -- admin id
)

redemptions (                          -- активации скидок
  id            serial PK,
  code          text UNIQUE,           -- 6 символов, он же в QR
  user_id       bigint FK -> users,
  partner_id    int FK -> partners,
  type          text,                  -- premium (daily — история до миграции 009)
  status        text,                  -- issued | used | expired
  issued_at     timestamptz,
  used_at       timestamptz,
  expires_at    timestamptz            -- issued_at + 30 мин
)

raffles (
  id            serial PK,
  title         text,
  prize         text,
  starts_at     timestamptz,
  ends_at       timestamptz,
  winner_id     bigint,
  status        text                   -- open | finished
)

raffle_entries (
  raffle_id     int FK,
  user_id       bigint FK,
  PRIMARY KEY (raffle_id, user_id)
)

broadcasts (
  id serial PK, text text, sent_at timestamptz, sent_count int
)
```

Индексы: `redemptions(code)`, `redemptions(partner_id, used_at)` — для статистики, `subscriptions(user_id, status)`.

---

## 3. Ключевые флоу

### 3.1 Оплата подписки (Kaspi, ручное подтверждение)
1. Пользователь жмёт «Оформить подписку 2000 ₸» → бот показывает номер Kaspi и просит скрин чека.
2. Пользователь шлёт фото → сохраняется в Storage, создаётся `subscription(status=pending)`.
3. Админу приходит карточка: чек + кнопки ✅ / ❌.
4. ✅ → `status=active`, пользователю уведомление; срок: `expires_at = GREATEST(текущий активный expires_at, now()) + 30 дней` — продление доступно в любой момент, оплаченные дни стекуются. ❌ → `rejected` + причина.
5. За 3 дня до `expires_at` — напоминание о продлении; после — `expired`, доступ закрывается.

**Продление:** чек при активной подписке принимается как заявка на продление (и Kaspi, и Stars — правило стекинга одно). Вторая подписка не создаётся: Stars продлевает активную строку UPDATE'ом, Kaspi-approve считает новый `expires_at` от максимума текущих активных.

Защита: не более 1 pending-заявки на пользователя (частичный уникальный индекс `subscriptions(user_id) WHERE status='pending'`, миграция 003); чек обязателен.

### 3.2 Активация скидки — механики

Четыре варианта, сравнение:

| Механика | Друг для кассира | Защита от фрода | Когда |
|---|---|---|---|
| **C. Наклейка QR на кассе, клиент сканирует сам** (deep link `t.me/bot?start=p_123`) | Ничего не делает | Средняя → высокая с защитами ниже. Наклейка = реклама бота + воронка регистрации | **MVP, основная** |
| **A. Клиент показывает код, кассир вводит** | Набрать 6 символов | Высокая: партнёр подтверждает визит вводом | MVP, фолбэк |
| **B. Кассир сканирует QR клиента** (Mini App `showScanQrPopup`) | Один тап + камера | Высокая | ✅ реализована как часть D |
| **D. Постоянная карта клиента** (личный QR, кассир сканирует → сервер сам применяет правила) | Один тап | Высокая: кассир видит имя+фото | ✅ **реализована** |

Почему C основная: партнёры — узкое место модели. Наклейка не требует от кассира ничего, работает без смартфона у кассы, а каждый скан незарегистрированного прохожего = новый пользователь в воронке.

**Флоу MVP (вариант C):**
1. На кассе у партнёра наклейка с QR → клиент сканирует камерой телефона → открывается бот с `start=p_123`.
2. Не зарегистрирован → онбординг, затем продолжение. Зарегистрирован → бот проверяет подписку и лимиты.
3. Бот показывает **экран активации**: название заведения, % скидки, имя клиента, сегодняшняя дата + **живые тикающие часы** + «знак дня» (эмодзи/цвет, который бот утром рассылает партнёрам). Экран живёт 5 минут.
4. Клиент показывает экран кассиру → кассир сверяет знак дня и тикающие часы → даёт скидку.
5. Параллельно партнёру в бот прилетает пинг: «Иван активировал скидку 10%, 14:32» — кассир может свериться со своим телефоном (полностью исключает подделку экрана). Визит записан автоматически.

**Фолбэк (вариант A)** — для партнёров, которые хотят сами подтверждать визиты: клиент жмёт «Активировать» в боте, получает код `A7K2M9` (TTL 30 мин), кассир вводит его в кабинете и видит имя клиента. Одноразовость — атомарный UPDATE:
```sql
UPDATE redemptions SET status='used', used_at=now()
WHERE code=$1 AND partner_id=$2 AND status='issued' AND expires_at > now()
RETURNING *;
```

**Вариант D (реализован): кассир сканирует персональный QR клиента.** Обратное направление варианта C — активирует кассир, клиенту сканировать ничего не нужно:
- У каждого клиента постоянный `users.qr_token` (миграция 007), генерируется лениво в `GET /api/me`. Страница «Мой QR» в Mini App (`/qr`) рисует стилизованный QR (`qr-code-styling`, коррекция H) с аватаркой из Telegram в центре (`GET /api/me/avatar` — байты отдаёт API, прямые file-ссылки Telegram светят токен бота); дизайн зависит от подписки: фирменный янтарный у подписчиков, графитовый без.
- QR кодирует `t.me/<bot>/app?startapp=c_<token>`: кассир сканирует из кабинета системным сканером Telegram (`qrScanner`) или обычной камерой — оба пути ведут на экран `/scan/<token>`, который зовёт `POST /api/partner/scan`.
- `/partner/scan` — зеркало `/api/activate`: тот же `redemption.activate()` (проверка подписки / лимит 1 визит в день), визит пишется сразу, клиенту уходит подтверждение в бот. Двойная активация «клиент отсканировал наклейку + кассир отсканировал клиента» гасится уникальным индексом дневного лимита.
- Анти-фрод: токен статичный, но скидку применяет сам кассир, видя имя клиента на экране результата; чужой скриншот QR лишь запишет визит владельцу токена.

**Анти-фрод правила (общие):**
- Лимит: 1 активация у одного партнёра в день на пользователя (настраивается per-partner). Атомарность против гонки — уникальный индекс `redemptions(user_id, partner_id, issued_on)`, где `issued_on` — локальный (Asia/Almaty) день выдачи (миграция 003).
- Скидка только по активной подписке — без неё API отвечает 402 (механика «скидка дня» удалена миграцией 009).
- Активация привязана к партнёру (deep link или код) — в другом заведении не работает.
- Экран активации: тикающие часы + дата + знак дня + имя владельца → скриншот и чужой экран палятся за секунды.
- Пинг партнёру в реальном времени — арбитр при любых сомнениях.
- Активации только в часы работы заведения (опционально, поле `work_hours`).

**Edge cases:**
- Отсканил дома «про запас» → экран живёт 5 минут, потом «Отсканируйте QR на кассе заново».
- Отсканил и ушёл без покупки → визит записан, статистика шумит. Партнёр видит пинги и может репортить; при желании партнёра — переключаем его на вариант A.
- Нет интернета у клиента на кассе → фолбэк A наоборот не поможет (тоже нужен интернет); кассир даёт скидку по подписке в профиле клиента (профиль кешируется ботом) или без скидки. Редкий случай.
- Несколько кассиров у партнёра → таблица `partner_staff`: владелец добавляет кассиров по telegram_id в кабинете, пинги активаций приходят владельцу и всем кассирам. Кассир гасит коды и видит статистику; пауза, смена скидки и сотрудники — только владелец.
- Наклейку сфотографировали и повесили в другом месте → безвредно: активация всё равно записывается на партнёра из QR.

### 3.3 Кабинет партнёра
- Вход: админ добавляет партнёра по telegram_id → роль `partner`. Кассиры получают ту же роль через `partner_staff`; при удалении кассира роль возвращается в `buyer`, если связей с заведениями не осталось (admin в обе стороны не трогается).
- Меню: «Ввести код клиента», «Статистика» (визиты за месяц, по дням), «Моя скидка» (владелец сам меняет % для подписчиков в пределах 10–15; остальные поля карточки — через админа), «Сотрудники» (только владелец).

### 3.4 Розыгрыш
- Админ создаёт розыгрыш; все пользователи получают кнопку «Участвовать».
- По `ends_at` планировщик выбирает случайного из `raffle_entries`, уведомляет всех.

### 3.5 Админка (в боте)
`/admin` → меню: партнёры (добавить/удалить), подписчики (список + поиск), заявки на оплату, розыгрыши, рассылка (с подтверждением перед отправкой; отправка батчами 25 msg/сек — лимит Telegram).

---

## 3.6 Карта функционала бота

### Покупатель
| Экран/функция | Детали |
|---|---|
| /start + онбординг | Приветствие → согласие на обработку данных (галочка) → телефон кнопкой «Поделиться номером». Deep links: `p_123` → сразу активация, `ref_456` → привязка реферала |
| Главное меню | Reply-клавиатура: Все скидки · Моя подписка · Розыгрыш · Пригласить друга · Помощь |
| Все скидки (по подписке) | Категории → список → карточка: адрес, часы, %, кнопка-геолокация |
| Моя подписка | Статус, дней осталось, кнопка продления, история визитов, **«Вы сэкономили N ₸»** |
| Пригласить друга | Реферальная ссылка; друг оформил подписку → +7 дней рефереру |
| Уведомления | Подписка истекает (за 3 дня), розыгрыши |
| Помощь | FAQ + «Написать админу» (форвард сообщения тебе) + «Пожаловаться на заведение» |

### Партнёр
| Функция | Детали |
|---|---|
| Пинги активаций | В реальном времени: имя, скидка, время |
| Статистика | Сегодня / неделя / месяц; новые vs повторные клиенты |
| Пауза скидки | Владелец приостанавливает («отпуск»), карточка скрывается из каталога |
| Моя карточка | % для подписчиков (10–15) владелец меняет сам мгновенно; название/категорию/адрес/часы/чек — заявкой с модерацией админа (partner_edits); координаты и логотип — только админ |
| Сотрудники | Владелец добавляет кассиров инвайт-ссылкой (одноразовая, 24 ч) или по @username; кассирам — пинги, ввод кода, статистика |
| Фолбэк-ввод кода | Для варианта A; доступен владельцу и кассирам |

### Админ
| Функция | Детали |
|---|---|
| Очередь чеков | Список pending с кнопками ✅/❌, не только push-уведомления |
| Партнёры | CRUD, привязка telegram_id, генерация QR для наклейки |
| Рассылка | Сегменты: все / подписчики / истёкшие. Предпросмотр + подтверждение |
| Розыгрыши | Создать, завершить (авто по дате), победитель публично |
| Модерация | /ban фродеров, жалобы клиентов на заведения |
| /stats | DAU, регистрации, конверсия в подписку, активации по партнёрам, churn |

### Системное
- Один обработчик /start разбирает все deep links.
- i18n-словарь (рус/каз) с первого дня, даже если казахский добавится позже.
- Заглушка «техработы», человеческие тексты ошибок.
- Rate limit на активации (антиспам), логирование всех денежных операций.

---

## 4. Структура проекта

```
ynemkz/
├─ bot/
│  ├─ main.py               # entrypoint, Dispatcher
│  ├─ config.py             # env: BOT_TOKEN, SUPABASE_URL/KEY, ADMIN_IDS, KASPI_PHONE
│  ├─ db.py                 # asyncpg pool / supabase client
│  ├─ middlewares/auth.py   # роль в handler'ы
│  ├─ handlers/
│  │  ├─ buyer.py           # старт, скидки, подписка, QR
│  │  ├─ partner.py         # ввод кода, статистика
│  │  └─ admin.py           # всё админское
│  ├─ services/
│  │  ├─ qr.py              # генерация кодов и QR
│  │  ├─ payments.py        # чек, подтверждение
│  │  ├─ raffle.py
│  │  └─ broadcast.py
│  └─ scheduler.py          # APScheduler jobs
├─ api/                     # FastAPI для Mini App
│  ├─ main.py
│  ├─ auth.py               # валидация initData
│  └─ routes/               # catalog, me, partner, admin
├─ miniapp/                 # React + Vite (см. раздел 4.5)
├─ migrations/              # SQL
└─ docker-compose.yml
```

Роли — middleware: смотрит `users.role` + `ADMIN_IDS` из env, кладёт в контекст. Никаких паролей — авторизация по telegram_id (ТЗ «смена доступов через админа» = админ меняет роль).

---

## 4.5 Фронтенд — Telegram Mini App (основной интерфейс)

**Решение (Фаза 2):** Mini App — основной интерфейс продукта. Бот — транспорт: онбординг (телефон + согласие), пинги партнёрам, уведомления, приём чеков Kaspi, Stars-платежи, фолбэк-активация.

**Стек:** React + Vite + TypeScript, UI — `@telegram-apps/telegram-ui` (нативный вид Telegram, авто-тема; требует React 18), SDK — `@telegram-apps/sdk-react`, карта — Leaflet + OpenStreetMap (без API-ключей; «Маршрут» — ссылка в 2GIS). Хостинг: фронт — Vercel, бэк (бот + API) — Railway. Роутинг — HashRouter (SPA на статике без настройки).

**API:** FastAPI в одном репо с ботом, общий слой `db.py` и `services/` (бизнес-логика активации — одна функция `redemption.activate()` для бота и API). Бот и API — два процесса, один docker-compose. Пинги/инвойсы из API — через aiogram `Bot` без polling.

**Авторизация:** Mini App шлёт `initData` в заголовке `Authorization: tma <initData>` → FastAPI валидирует HMAC-подпись (ключ = HMAC-SHA256("WebAppData", bot_token)) → telegram_id → роль из `users`. Паролей нет, подделка невозможна. Кэш валидации 5 мин, initData старше часа отклоняется.

### Матрица: что в боте, что в Mini App

| Функция | Бот | Mini App |
|---|---|---|
| Онбординг (телефон, согласие) | ✅ основной | → отправляет в бот |
| Активация по наклейке | фолбэк (`?start=p_id`) | ✅ основной (`?startapp=p_id`) |
| Каталог, карта, карточки | текстовый фолбэк | ✅ основной |
| Подписка: статус, история, «сэкономлено» | фолбэк | ✅ основной |
| Оплата Kaspi (чек) | ✅ (фото чека — только чат) | кнопка → в бот |
| Оплата Stars | обработка платежа | ✅ кнопка + openInvoice |
| Пинги партнёру, уведомления | ✅ только бот | — |
| Кабинет партнёра, ввод кода | фолбэк (`/code`) | ✅ основной |
| Админка: чеки, метрики, календарь | фолбэк (команды) | ✅ основной |
| Рассылки, /ban, добавление партнёров | ✅ только бот | Фаза 2б |

### Активация через Mini App (вариант C, основной флоу)

1. Наклейка на кассе: QR → `t.me/<bot>/app?startapp=p_123` (Direct Link Mini App).
2. Mini App открывается сразу → `POST /api/activate {partner_id}` → сервер применяет правила (подписчик — premium-% везде; без подписки — 402 → экран «оформите подписку») и пишет визит (`status=used`).
3. Экран активации: заведение, −%, имя, дата, знак дня, **живые тикающие часы по серверному времени** (offset от `server_time` — перевод часов на телефоне не поможет), гаснет через 5 минут.
4. Пинг партнёру в бот — как раньше.
5. Незарегистрированный: API вернёт 403 → экран «запустите бота» → онбординг в боте → бот-фолбэк активации доступен сразу из чата.

### Оплата Telegram Stars

`POST /api/stars-invoice` → `createInvoiceLink(currency=XTR)` → Mini App `invoice.open()` → бот ловит `pre_checkout_query` (отклоняет забаненных и незарегистрированных ДО списания; активная подписка не блокирует — это продление со стекингом, раздел 3.1) и `successful_payment` (обрабатывается даже для забаненных — деньги уже списаны) → подписка активна мгновенно, без админа. `subscriptions.payment_method='stars'`, `tg_charge_id` — для возвратов; он же уникален (частичный индекс, миграция 003) — повторная доставка `successful_payment` идемпотентна, вторая подписка не создаётся.
Trade-off: комиссия Telegram ~30% + вывод в валюте (нужна карта/крипта) vs Kaspi без комиссии, но с ручной проверкой. Цена в Stars — `SUBSCRIPTION_PRICE_STARS` (компенсирует комиссию).

### Экраны (реализовано в скелете)

| Экран | Содержимое |
|---|---|
| Главная | Поиск, фильтр-чипы категорий, список партнёров (лого, %) |
| Заведение | Лого, %, адрес, часы, мини-карта, «Маршрут в 2GIS» |
| Карта | Пины всех партнёров (Leaflet/OSM), «📍 Рядом со мной» (геолокация) |
| Активация | Тикающие часы, знак дня, имя; истечение через 5 мин; 403 → в бот с сохранением partner_id |
| Профиль | Подписка + прогресс, оплата Stars/Kaspi, «сэкономлено N ₸», история визитов |
| Кабинет партнёра | Моя карточка + пауза-тумблер, визиты (+новые/повторные), бар-чарт 30 дней, ввод кода, лента активаций |
| Админ: Сводка | Метрики + конверсия, очередь чеков с ✅/❌ |
| Админ: Партнёры | Создание, редактор (%, адрес, часы, координаты, владелец), вкл/пауза, QR-наклейка (PNG) |
| Админ: Люди | Подписчики с поиском, бан/разбан, возврат Stars |
| Админ: Рассылка | Сегмент → текст → предпросмотр получателей → отправка |

### Endpoints
```
Клиент:   GET /api/catalog?category=   GET /api/partners/{id}
          GET /api/map                 GET /api/me               GET /api/me/visits
          POST /api/activate           POST /api/stars-invoice
Партнёр:  GET /api/partner/stats       GET /api/partner/me       GET /api/partner/activations
          POST /api/partner/redeem     POST /api/partner/pause   POST /api/partner/discount
          GET/POST /api/partner/staff  DELETE /api/partner/staff/{telegram_id}
          POST /api/partner/staff/invite (одноразовый токен → t.me/<bot>?start=staff_<token>)
          GET/POST/DELETE /api/partner/edit (заявка на изменение карточки)
Админ:    GET /api/admin/stats         GET/POST /api/admin/receipts(+/{id}/decide)
          GET/POST/PATCH /api/admin/partners(+/{id})   GET /api/admin/partners/{id}/qr (PNG)
          GET /api/admin/subscribers?q=                POST /api/admin/users/{id}/ban
          POST /api/admin/subscriptions/{id}/refund
          POST /api/admin/broadcast (dry_run → предпросмотр)
          GET /api/admin/partner-edits                 POST /api/admin/partner-edits/{id}/decide
```

### Бэклог трендов (приоритет сверху вниз)

| Механика | Суть | Фаза |
|---|---|---|
| Геймификация | Баллы за визиты, стрики («5 дней подряд»), обмен баллов на дни подписки | 2б |
| Отзывы и рейтинги | Оценка только после реального визита (по `redemptions`), рейтинг в каталоге | 2б |
| Шеринг экономии | «Я сэкономил N ₸» — карточка в стори/чат, реферальная ссылка внутри | 2б |
| Избранное + поиск | Закладки заведений, поиск по названию | 2б |
| Stories партнёров | Карусель акций на главной, канал продвижения для партнёров | 2в |
| Личный QR клиента | Механика D: кассир сканирует карту клиента | 2в |
| Self-service партнёра | Заявки на изменение карточки/акций с модерацией | 3 |
| Сканер кассира | `showScanQrPopup` — механика B | 3 |

---

## 5. Нагрузка и надёжность

Экибастуз ~120 тыс. населения → реалистично сотни–тысячи пользователей, десятки визитов/день. Это **ничтожная нагрузка**: один процесс бота (long polling, без webhook) + Supabase free tier хватит с запасом. Масштабирование не проектируем — незачем.

Надёжность:
- Docker + `restart: always` на VPS.
- Бэкап БД: Supabase делает сам (PITR на платном; на free — pg_dump кроном раз в день).
- Логи операций с деньгами (подтверждения подписок) — не удалять, только статусы.

---

## 6. Trade-offs

| Решение | Почему | Цена |
|---|---|---|
| Long polling вместо webhook | Не нужен домен/SSL, проще деплой | Ничего при такой нагрузке |
| Ручное подтверждение Kaspi | API переводов физлицам нет | Твоё время; при росте — Kaspi Pay API (нужно ИП + договор) |
| Наклейка QR на кассе как основная механика | Ноль действий от кассира, реклама бота, воронка регистрации | Скриншот-фрод (закрыт знаком дня + часами + пингом партнёру), шум в статистике |
| Всё в боте, Mini App потом | MVP за дни, а не недели | Витрина менее красивая на старте |
| Supabase, не голый Postgres | Storage для чеков + бэкапы + дашборд из коробки | Вендор; уйти легко (обычный Postgres) |

---

## 7. План MVP → полная версия

**Фаза 1 — MVP (1–2 недели):** регистрация, оплата чек→подтверждение, наклейки QR на кассах + экран активации + пинг партнёру, фолбэк с кодом, счётчик визитов, админ: добавить партнёра/список подписчиков. Дизайн и печать наклеек — в чеклист запуска. (Механика «скидка дня» была в MVP, удалена миграцией 009 — скидки только по подписке.)

**Фаза 2а (скелет готов):** Mini App — основной интерфейс: витрина + карта заведений, экран активации через `startapp`, профиль с оплатой Stars/Kaspi, кабинет партнёра, админка (чеки + метрики). API (FastAPI). Деплой: Railway (bot+api) + Vercel (Mini App).

**Фаза 2б:** геймификация (баллы/стрики), отзывы после визита, шеринг экономии, избранное+поиск, розыгрыши.

**Фаза 3 (при росте):** Kaspi Pay API (автоплатежи), stories партнёров, личный QR клиента, self-service партнёров, несколько городов (поле `city`).

**Пересмотреть при росте:** ручное подтверждение оплат (узкое место — ты), long polling → webhook при >10k пользователей, лимиты рассылки.
